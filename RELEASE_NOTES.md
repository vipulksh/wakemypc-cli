## v1.1.0 - Upload with Github

- `upload` command can now use `--github` flag to use files from github release directly instead of requiring local firmware directory
- `status` now includes firmware version